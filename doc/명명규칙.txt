명명규칙 (내 마음대로 정한 규칙)
상수형 구조체(struct)의 이름은 C(Constant)로 시작한다.
그 외 일반적인 구조체의 이름은 S(Structure)로 시작한다.
함수(func)의 이름은 F(Function)으로 시작한다.

상수형 구조체를 생성하는 func은 "F(상수형 구조체 이름)생성"으로 한다.
모든 구조체의 값을 읽는 메소드 이름은 "G"(Get)으로 한다.
모든 구조체의 값을 설정하는 메소드 이름은 "S"(Set)으로 한다.